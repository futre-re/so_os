// fn schedule(processes: &mut Vec<ProcessControlBlock>) {
//     let next_process = select_next_process(processes);
//     if let Some(next) = next_process {
//         context_switch(&mut processes[0], &next);
//     }
// }
